[project]
name = "ai-sentiment-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "scikit-learn",
    "firebase-admin"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]